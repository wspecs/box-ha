server {
  listen CURRENT_IP:80;
ALLOW_HTTP_IPS
  deny all;

  root /var/www/html;
  server_name _;
  index index.html index.htm index.nginx-debian.html;
  location / {
    try_files $uri $uri/ =404;
  }
}
